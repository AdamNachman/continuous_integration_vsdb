--I'm inserting this comment so that I can test the CREATE of a new table with this comments
CREATE TABLE [MySchema].[TableCommentPrefix]
(
	[Col1] INT NOT NULL, 
	[Col2] INT NULL, 
	[Col3] INT NOT NULL
);